{
  primaryUser = {
    # used as the actual linux user name/home directory
    username = "";
    # used for git config and user description
    fullname = "";
    # used for git config
    email = "";
  };

  # Get this from the autogenerated boot.initrd.luks in
  # /etc/nixos/configuration.nix
  # (Just the Uuid part, not prefixed with a path or "luks-")
  luksDeviceUuid = "<uuid>";

  # What proportion of the width of your screen is suitable for a
  # "landscape" window?
  # Used to set the initial window size of some apps.
  landscapeWidthProportion = 1. / 1.;

  # If enabled, sets up kopia client for trouv's backup server.
  # Don't use this if the machine is not in trouv's local network.
  kopiaClientVatia = false;

  # If enabled, sets up nvidia drivers.
  nvidia = false;
}
